import { post } from 'network';
import { SocialLoginD } from 'types/auth';
import { osName, deviceType } from 'react-device-detect';

export const getAppsApi = async ({ token }: { token: string }) => {
  // return post({ route: 'z' });
};
